# Default values for Maven projects.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 2
image:
  pullPolicy: IfNotPresent
  repository: ls-docker.repo.clarivate.io/spring-boot-example
  tag: latest
service:
  name: java
  type: ClusterIP
  externalPort: 80
  internalPort: 4567
resources:
  limits:
    cpu: 100m
    memory: 128mi
  requests:
    cpu: 100m
    memory: 128mi
ingress:
  enabled: false
